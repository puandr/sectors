import React from 'react';
import ReactDOM from 'react-dom';
import SectorsForm from './components/SectorFrom'; 

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
      <SectorsForm />
    </React.StrictMode>
  );
